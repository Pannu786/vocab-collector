import { TextField, Button } from '@mui/material';
const Edit = ({
  words,
  setWords,
  word,
  meaning,
  setWord,
  setMeaning,
  editWord,
  setEditWord,
  editMeaning,
  setEditMeaning,
}) => {
  return <div>Edit</div>;
};

export default Edit;
